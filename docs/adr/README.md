# Architecture decisions

Generated by `./gen-index.sh` — edit the ADRs, not this file.

An ADR here is a decision with its evidence attached: what forced it, what was rejected, what
would prove it wrong. Status is written in the ADR itself and says what is actually TRUE, not what
was intended — "PROPOSED" means no code, and a claim only becomes "PROVEN" when something ran.

| # | Decision | Status |
|---|---|---|
| [0001](0001-agent-spool-deterministic-tab-targeting.md) | Deterministic per-session tab targeting for the agent sendkeys spool | Accepted — IMPLEMENTED & VERIFIED 2026-07-28 |
| [0002](0002-teams-audio-call-agent.md) | Teams audio-call agent (speak + listen in a live meeting) | Layers 1, 2 AND 3 BUILT and verified — the loop runs end to end. |
| [0003](0003-chrome-agent-on-a-server.md) | chrome-agent on a server: headless fork, human login over a time-boxed share | HALF BUILT AND HALF PROVEN, 2026-09-12. The CLI side exists: `up --headl |
| [0004](0004-site-definitions-as-installed-assets.md) | Site definitions are data, shipped with the skill and installed where they can be edited | BUILT AND PROVEN, 2026-09-12. 19 site definitions ship; `scripts/sites.p |
| [0005](0005-profile-and-session-lifecycle.md) | Profile and session lifecycle are CLI verbs, and logout is one of them | BUILT, 2026-09-12. All six verbs exist. `logout` was tested on all three |
| [0006](0006-chrome-agent-cli-separate-from-the-fork.md) | A distributable `chrome-agent` CLI, separate from the fork it drives | PARTLY BUILT — the discussion is still open. `doctor` exists and answers |
| [0007](0007-releases-across-operating-systems.md) | Releasing the fork across operating systems | PROPOSED, with tooling and the first artifact (2026-09-13). The Linux ar |
| [0008](0008-producing-the-linux-build.md) | Producing the Linux build of the fork | DONE, 2026-09-13. The first Linux build of this fork exists and passes e |
| [0009](0009-the-public-agent-protocol.md) | The public agent protocol: instances, media, per-tab streams, and consent | PROPOSED. Nothing here is built. It supersedes the X/VNC half of ADR 000 |
| [0010](0010-the-client-is-one-go-binary.md) | The client is one Go binary, and bash retires | SLICES 1-2 BUILT, 2026-09-13. One static binary, ~3.5 MB, cross-compiled |
| [0011](0011-browser-state-export-and-import.md) | Exporting and importing browser state (cookies, storage) as an explicit credential | PROPOSED. Nothing is built. This is the highest-risk decision in the set |

## How they hang together

```
0001 spool + tabId ─────────────► 0009 public protocol ──► 0010 Go client
0002 audio in/out ──────────────►      (media, grants)     (bash retires)
                                            │
0004 site definitions ──► 0005 lifecycle ───┤
     (installed wins)      (login/logout)   ├──► 0011 state export/import
                                            │     (cross-OS sessions)
0006 CLI vs engine ─────────────────────────┘
                                            
0008 Linux build ──► 0003 server + login ──► 0007 releases across OS
     (done)              (unblocked)             (nothing signed yet)
```

**The load-bearing ones to read first:** 0009 (what other people's software may do), 0007 (what a
release is, and why the 109 MB macOS `.app` is not one), and 0011 (why an exported cookie jar is a
credential and not a file).
