{
  "domain": "facebook.com",
  "aliases": [
    "facebook",
    "fb",
    "meta"
  ],
  "home": "https://www.facebook.com/",
  "login": {
    "url": "https://www.facebook.com/login/",
    "note": "Manual, by a human, in this profile — always (ADR-0008). Email/phone plus password, then very commonly a device-approval or code step; Facebook is the quickest of these sites to lock an account it thinks is automated.",
    "twofa": true
  },
  "logout": {
    "method": "cookies",
    "note": "UNVERIFIED, and deliberately not guessed. Facebook's logout is a POST carrying the per-session fb_dtsg token from the account menu; the old /logout.php?h=<hash> GET needs that same per-page hash, so a typed URL does not work. Clear the profile's facebook.com cookies instead."
  },
  "auth": {
    "probe_url": "https://www.facebook.com/",
    "probe_js": "const c = (() => { try { return document.cookie; } catch (e) { return ''; } })();\nconst m = c.match(/\\bc_user=(\\d+)/);\nreturn m ? {signed_in:true, as:'fbid:'+m[1]} : {signed_in:false, why:'no c_user cookie'};"
  },
  "read": {
    "verb": "facebook:feed",
    "fixture_url": "https://www.facebook.com/"
  },
  "write": [],
  "traps": [
    "One shared browser, no mutex. Two lanes attach to a nondeterministic tab. Run `chrome-agent goto <url>` then `chrome-agent status` before believing any read.",
    "A logged-out feed returns postCount:0 with no error — 'the verb ran' is not 'the verb works'. Zero items here means signed out or drifted, never 'nothing to see'.",
    "The probe reads `c_user`, which is readable but is only an account ID — it can never name a person, and it goes stale in a profile whose session cookie has expired while c_user lingers. Treat a c_user-only verdict as weaker evidence than the API probes the other sites use.",
    "No write verb exists for this site. Do not improvise one by driving the DOM.",
    "Verification rule: read the artifact back from the live page. A 2xx proves nothing here, and on some of these sites neither does a 5xx."
  ],
  "status": "unverified",
  "source": "hand",
  "notes": "HYPOTHESIS. Converted from the CLI's `_auth_probe_js` case branch and playbooks/facebook.com/ (meta.md says last_verified: never). As of 2026-09-12 the auth probe reports signed OUT in this profile and facebook:feed returns 0 items, so nothing here has been driven green. `chrome-agent verify facebook.com` plus a live `auth` are what promote this to verified.",
  "probe_checked": "2026-09-14: Go client `auth` ran live against the owner profile and returned a clean signed_out verdict (no c_user cookie). Signed-OUT path proven again; signed-IN path still a hypothesis (profile is not signed in here)."
}
